/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AppSettingsRequest } from '../../models/app-settings-request';
import { AppSettingsResponse } from '../../models/app-settings-response';

export interface ApiAppSettingsPut$Json$Params {
      body: AppSettingsRequest
}

export function apiAppSettingsPut$Json(http: HttpClient, rootUrl: string, params: ApiAppSettingsPut$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<AppSettingsResponse>> {
  const rb = new RequestBuilder(rootUrl, apiAppSettingsPut$Json.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AppSettingsResponse>;
    })
  );
}

apiAppSettingsPut$Json.PATH = '/api/AppSettings';
