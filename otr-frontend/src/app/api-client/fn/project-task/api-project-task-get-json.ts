/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProjectTask } from '../../models/project-task';

export interface ApiProjectTaskGet$Json$Params {
}

export function apiProjectTaskGet$Json(http: HttpClient, rootUrl: string, params?: ApiProjectTaskGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ProjectTask>>> {
  const rb = new RequestBuilder(rootUrl, apiProjectTaskGet$Json.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ProjectTask>>;
    })
  );
}

apiProjectTaskGet$Json.PATH = '/api/ProjectTask';
