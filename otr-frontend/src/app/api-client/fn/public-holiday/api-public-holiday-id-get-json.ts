/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PublicHoliday } from '../../models/public-holiday';

export interface ApiPublicHolidayIdGet$Json$Params {
  id: (number | string);
}

export function apiPublicHolidayIdGet$Json(http: HttpClient, rootUrl: string, params: ApiPublicHolidayIdGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<PublicHoliday>> {
  const rb = new RequestBuilder(rootUrl, apiPublicHolidayIdGet$Json.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PublicHoliday>;
    })
  );
}

apiPublicHolidayIdGet$Json.PATH = '/api/PublicHoliday/{id}';
