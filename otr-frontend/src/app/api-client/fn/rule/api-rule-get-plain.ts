/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Rule } from '../../models/rule';

export interface ApiRuleGet$Plain$Params {
}

export function apiRuleGet$Plain(http: HttpClient, rootUrl: string, params?: ApiRuleGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Rule>>> {
  const rb = new RequestBuilder(rootUrl, apiRuleGet$Plain.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Rule>>;
    })
  );
}

apiRuleGet$Plain.PATH = '/api/Rule';
