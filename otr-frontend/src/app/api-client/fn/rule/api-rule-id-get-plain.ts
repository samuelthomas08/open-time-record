/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Rule } from '../../models/rule';

export interface ApiRuleIdGet$Plain$Params {
  id: (number | string);
}

export function apiRuleIdGet$Plain(http: HttpClient, rootUrl: string, params: ApiRuleIdGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Rule>> {
  const rb = new RequestBuilder(rootUrl, apiRuleIdGet$Plain.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Rule>;
    })
  );
}

apiRuleIdGet$Plain.PATH = '/api/Rule/{id}';
